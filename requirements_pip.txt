# Tiaozhanbei2.0 智能管道追踪系统 - PIP依赖列表
# 基于实际项目使用情况的精简依赖列表
# 适用于 pip install -r requirements_pip.txt

# =============================== 核心运行时依赖 ===============================

# 计算机视觉 - OpenCV
opencv-python>=4.8.0,<5.0.0

# 数值计算 - NumPy  
numpy>=1.21.0,<2.0.0

# Intel RealSense 深度相机
pyrealsense2>=2.50.0

# 串口通信 (机器人控制)
pyserial>=3.5.0,<4.0.0

# Web界面框架
Flask>=3.0.0,<4.0.0

# =============================== 安装说明 ===============================

# 快速安装:
#   pip install -r requirements_pip.txt

# 分步安装 (推荐):
#   pip install opencv-python numpy flask pyserial
#   pip install pyrealsense2  # 需要先安装RealSense SDK

# Conda环境安装:
#   conda install opencv numpy flask
#   pip install pyrealsense2 pyserial

# =============================== 系统要求 ===============================

# Python: 3.8+
# 操作系统: Ubuntu 18.04+ / Windows 10+ / macOS 10.15+
# 硬件: Intel RealSense D455 深度相机

# =============================== 注意事项 ===============================

# 1. pyrealsense2 依赖:
#    - 需要先安装 Intel RealSense SDK 2.0
#    - Ubuntu: sudo apt install librealsense2-dev
#    - Windows: 下载官方SDK安装包
#    - macOS: brew install librealsense

# 2. OpenCV 注意:
#    - 使用 opencv-python 而非 opencv-contrib-python
#    - Jetson设备建议使用系统预装版本

# 3. 串口权限 (Linux):
#    - sudo usermod -a -G dialout $USER
#    - 重新登录生效

# =============================== 可选依赖 ===============================

# 以下依赖可根据需要单独安装:

# 点云处理 (高级功能):
# open3d>=0.17.0

# 科学计算 (数学运算):  
# scipy>=1.9.0

# 深度学习 (AI增强):
# torch>=2.0.0
# torchvision>=0.15.0

# 开发工具:
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
# scikit-learn>=1.3.0

# ============================ 平台特定 ============================
# Windows 特定
# pywin32>=306  # 仅 Windows

# Linux 特定依赖会自动处理

# ============================ 版本说明 ============================
# Python 版本要求: >= 3.8
# 操作系统: Windows 10/11, Ubuntu 18.04+, macOS 10.15+
# 相机: Intel RealSense D400 系列
# 机器人: DJI RoboMaster C 板
